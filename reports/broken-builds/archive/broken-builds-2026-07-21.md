Report written to `reports/broken-builds/current.md`. All 9 monitored repos encountered collection errors ("Failed to query repo API"), indicating a systemic issue with the GitHub API collector rather than actual CI build failures. The most likely cause is a missing or expired GITHUB_TOKEN environment variable.
